---
created: 2026-08-16
updated: 2026-08-16
tags: [v1.9, semantics, sir, verify, differential]
aliases: [Semantic Preservation, Preservação Semântica]
---

# 29.13 — Semantic Preservation

> *Preservar a **semântica** de uma arquitetura, não apenas executar seus binários.*
> Extensão do Path v1.9 sobre o pipeline v1.7/v1.8 (`x86 → SIR → multi-ISA`).

Preservation Layer formalizado em `base-vault/isa/` (níveis P0–P5, matriz + snapshots
gerados por `base recomp report`).

## Três níveis de confiança

| Nível | Pergunta | Mecanismo |
|-------|----------|-----------|
| **literal** | os mesmos bytes recuperam a mesma forma? | `decode(encode(SIR)) == SIR` |
| **semantic** | a forma recuperada tem o mesmo significado? | `semantic_key(SIR) == semantic_key(SIR′)` (domínio 32-bit) |
| **differential** | produz o mesmo estado arquitetural? | `execute_reference(SIR, state, width, endian) == execute_isa(decode(encode(SIR)), state)` |

Palavra não reconhecida → `Op::Unknown` (gap), nunca mis-decode silencioso.

## Componentes (`base-recomp`)

| Módulo | Papel |
|--------|-------|
| `semantics.rs` | Catálogo serde das 11 ISAs (regs, endianness, delay slots, flags, ABI, quirks, status de encode) |
| `decode.rs` | Decoders subset (MIPS/PPC/SuperH/Alpha/PA-RISC/ColdFire/ARM/AArch64/SPARC/x86_64) que invertem os encoders |
| `verify.rs` | Round-trip + `semantic_key` + cobertura por dimensão + `differential_kind` |
| `semexec.rs` | Execução de referência: `MachineState { gpr, pc, flags, mem }`, `load`/`store`, push/pop, `differential_sweep` |

## Bugs reais que o verifier encontrou

1. **PPC `r0`**: lê como 0 no campo RA de `addi` → `MovImm{r0}` e `AddImm{r0}` colidiam. Fix: VReg → `r3..r31`.
2. **ColdFire `Dn`**: bits 5-3 sem `<< 3` no grupo `addi/subi/clr/addq/subq/push` → `clr.l d3` virava `clr.l (a3)+`. Só D0 passava no roundtrip antigo (VReg 0).
3. **Alpha LDA sign-extension**: disp negativo → `add -1` real a 64 bits ≠ `add 0xFFFFFFFF` da spec → `semantic` passa, `differential` falha (gap documentado, não escondido).

## Cobertura por dimensão (nunca um score único)

`base recomp verify --all`:

```text
| ISA | enc | dec | literal | semantic | exec | differential | status |
| mips     | 67% | 67% | 67% | 67% | 83% | 67% | PARTIAL |
| ppc      | 67% | 67% | 58% | 67% | 83% | 67% | PARTIAL |
| sh4      | 67% | 67% | 58% | 67% | 83% | 33% | PARTIAL |
| alpha    | 67% | 67% | 58% | 67% | 83% | 50% | PARTIAL |
| parisc   | 17% | 17% | 17% | 17% | 83% | 17% | PARTIAL |
| coldfire | 83% | 83% | 83% | 83% | 83% | 83% | PARTIAL |
| aarch64  | 67% | 67% | 67% | 67% | 83% | 33% | PARTIAL |
| arm      | 67% | 67% | 58% | 67% | 83% | 33% | PARTIAL |
| sparc    | 33% | 33% | 33% | 33% | 83% | 33% | PARTIAL |
| x86_64   | 83% | 83% | 83% | 83% | 83% | 83% | PARTIAL |
| m88k     |  0% |  0% |  0% |  0% | 83% |  0% | NONE    |
```

`abi/privileged/mmu/system` = `0%` (não modelados). Sweep gerado: coldfire 256/256,
x86_64 256/256, mips 176/176, aarch64 136/136, arm 104/104, sparc 56/56, alpha 160/176
(16 mismatches só em add/sub_imm negativos — gap Alpha). AArch64/ARM differential 33%:
immediates de borda `0xFFFFFFFF` não cabem em MOVZ (16-bit)/ADD (12-bit) nem em ARM imm8
— limitação honesta, mesma do SH. ARM `literal < semantic`: `Clear` vira `MOV #0`;
rotate/S=1 são gaps. SPARC: só `Nop/Ret/MovImm/Clear` (%l0..%l7), imm13 sign-extend faz
edge `0xFFFFFFFF` (=−1) encodar — differential 33%. x86_64: imm32 total + push/pop →
83% em todas dimensões; só `call`/`jmp` faltam (reloc/linker — gap honesto).

## Fila

- [x] Catálogo semântico 11 ISAs · 14 targets · encoders · emitters · decoders (10) · verifier · semexec · sweep
- [ ] Flags (CCR vs T vs icc) · branches · load/store no SIR · M88k/IA-64/i860 · encoder gaps restantes (x86 call/jmp)

[[29.00 - Index]]
